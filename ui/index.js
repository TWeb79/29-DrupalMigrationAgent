import React from 'react';
import ReactDOM from 'react-dom/client';
import DrupalMind from './DrupalMindUI';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DrupalMind />
  </React.StrictMode>
);
